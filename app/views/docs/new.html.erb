<div class="text-center">
  <h2>Archivo CSV</h2>
  <div class="alert alert-primary" role="alert">
    Seleccionar y subir un archivo CSV con los campos de nombre, correo_electronico, telefono.
  </div>
</div>
<div class="text-center">
  <%= form_tag(docs_path(), { :multipart => true }) do %>
    <%= label_tag(:file, "Subir archivo CSV:") %>
    <%= file_field_tag "file" %>
    <%= submit_tag("Subir") %>
  <% end %>
</div>